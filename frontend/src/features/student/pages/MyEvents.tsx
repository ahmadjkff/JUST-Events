import { useTitle } from "../../../hooks/useTitle";

function MyEvents() {

  useTitle("My Events - JUST Events");

  return (
    <div>
      <h1>MyEvents</h1>
    </div>
  );
}

export default MyEvents;
